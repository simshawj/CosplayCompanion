<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/primary</item>
        <item name="android:textColorHint">@color/primary</item>
        <item name="android:editTextColor">@color/primary</item>
        <item name="colorButtonNormal">@color/secondary</item>
    </style>

    <style name="AppTheme.TextHeader">
        <item name="android:paddingBottom">@dimen/text_header_padding_bottom</item>
    </style>

    <style name="AppTheme.FormSingleButton">
        <item name="android:layout_marginTop">@dimen/form_button_margin_top</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
